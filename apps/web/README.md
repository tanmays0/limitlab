# LimitLab web

Next.js demo dashboard and `/api` rate-limit routes.

```bash
npm install && npm run dev
```

Unset `NEXT_PUBLIC_API_URL` to use same-origin `/api`. Set it to a FastAPI base URL to use Redis-backed endpoints.

See the repository root [README.md](../../README.md).
